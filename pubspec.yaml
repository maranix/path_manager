name: path_manager
description: Plugin for getting commonly used locations on host file system via FFI
homepage: https://github.com/maranix/path_manager
repository: https://github.com/maranix/path_manager
issues: https://github.com/maranix/path_manager/issues
version: 0.0.1

environment:
  sdk: ^3.12.1

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.4
  objective_c: ^9.3.0

dev_dependencies:
  ffi: ^2.1.4
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0

topics:
  - files
  - path-provider
  - paths
  - path-manager
  - device-directory
