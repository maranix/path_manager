# Run with `dart run ffigen --config ffigen.yaml`.
name: PathManagerBindings
description: |
  Bindings for `src/path_manager.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/path_manager_bindings_generated.dart'
headers:
  entry-points:
    - 'src/path_manager.h'
  include-directives:
    - 'src/path_manager.h'
ffi-native:
comments:
  style: any
  length: full
